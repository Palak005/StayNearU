<% layout('/layouts/boilerplate') %>

<div class="row mt-3">
    <div class="col-8 offset-2">
    <h1 class="new-head">SignUp</h1>
    <br>
    <form action="/users/signUp" method="POST" class="needs-validation" novalidate>
        <div class="mb-4 head">
            <label for="title" class="form-label">Enter Username</label>
            <input placeholder="Enter username" type="text" class="form-control" name="username" required>
            <div class="invalid-feedback">
                *Username is required
            </div>
        </div>

        <div class="mb-4 head">
            <label for="description" class="form-label">Enter Email</label>
            <input placeholder="Enter email" type="text" class="form-control" name="email" required>
            <div class="invalid-feedback">
                *Email is required
            </div>
        </div>

        <div class="mb-4 head">
            <label for="image" class="form-label">Enter Password</label>
            <input placeholder="Enter Password" type="text" class="form-control" name="password" required> 
            <div class="invalid-feedback">
                *Password is required
            </div>   
        </div>

        <button class="btn btn-dark add-btn" type="submit" >Sign Up</button>
    </form>
    <br><br>
    </div>
</div>